
title: Website Name
description: A website for uplooading content
age: 30

tags:
  -technoogy
  -images
  -creative

address:
  street: MG Road
  city: Nashik
  state: MH

  value: ${{env.VALUE}}
  variable: ${{github.VARIABLE_NAME}}

  # name of app
  name: Github action ofr
  on: push
      branches: [master]
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        -uses: actions/checkout@v3
        -name: Print Hello World
          run: node --version
                